#include <stdio.h>

/**
 * main - Prints a string using printf
 * 
 * Description: This prints the string "with proper grammar, but the
 * outcome is a piece of art,"
 * Return: 0 (on success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
