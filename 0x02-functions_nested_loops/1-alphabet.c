#include "main.h"

/**
 * main - Prints the alphabet in lower case, followed by a new line.
 *
 * Return: 0 (Success)
 */

int main(void)
{
	for (l = 'a'; l <= 'z'; l++)
	{
		_putchar(l);
	}
	_putchar('\n');

	return (0);
}
