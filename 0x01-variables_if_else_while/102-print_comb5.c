#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints all possible different combinations of two 2-digit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (i = 0; i < 100; i++)
		putchar('\n');

	return (0);
}
